import React from "react";
import { Ellipsis } from "react-css-spinners";

const LoadingSpinner = () => {
  return (
    <div>
      <Ellipsis color="black" size={60} />
    </div>
  );
};

export default LoadingSpinner;
